# Reuse the upstream runtime/dependency image. The frontend is built on the
# host with ERD_WEB_BUILD=1 and copied together with the MCP/server source.
FROM bekenweb/erd-builder-pro:latest

WORKDIR /app

COPY . .
